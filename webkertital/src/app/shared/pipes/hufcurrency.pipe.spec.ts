import { HufcurrencyPipe } from './hufcurrency.pipe';

describe('HufcurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new HufcurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
